# 轨迹一致性分析工具

主要任务对 Android World 评测框架下，测试四个互不干扰的模拟器并行执行这些任务产生轨迹是否完全一致，尝试使用temperature和seed参数控制模型输出。

---

## 整体流程

```
并行任务执行层                   分析Pipeline
parallel_runner_vllm ──▶   analyze_consistency   ──▶ trajectory_consistency_chart.png
       ↓                                          ──▶ trajectory_analysis_report.txt
   run.py                                         ──▶ trajectory_consistency.json
       ↓                                          ──▶ unknown_actions_report.txt
 trace.json + images   
  
                            visualize_trace       ──▶ grouped_report.html (带一致性标签)
                         
```

---

## 一、任务执行层

### run.py
Android World 官方的单次评测入口。

### parallel_runner_vllm.py
多实例并行调度器，基于 vLLM 本地推理服务，统一管理多台 Android 模拟器。

**核心机制：**

1. **模式（Mode）** — 定义一组实验参数，按顺序执行
2. **资源池（Resource Pool）** — 4 组 (adb_port, grpc_port) 对应 4 台模拟器
3. **任务队列** — 从 `all_tasks.txt` 读取所有任务，每 4 个一组并行执行

**模式定义（MODES）：**
```
| 温度 (temp) | 随机种子 (random_seed) | 模式名称) |

| 0.0 | False | `T0.0_FixedSeed`  |
| 0.0 | True  | `T0.0_RandomSeed` |
| 1.0 | False | `T1.0_FixedSeed`  |
| 1.0 | True  | `T1.0_RandomSeed` |
```
**执行流程（每个模式）：**

```
 清空并重建 outputs/、logs/ 目录
 从 all_tasks.txt 遍历所有任务
   └─ 每个任务启动 MAX_PARALLEL 个进程
      └─ run_single_instance()
         ├─ 启动模拟器（从快照恢复）
         ├─ 设置 ADB + gRPC + 无障碍服务
         ├─ 运行 run.py（注入 LLM_TEMPERATURE、随机种子）
         ├─ 等待 run.py 结束
         ├─ 关闭并清理模拟器
         └─ 记录执行日志到 task_summary.log
 所有任务完成后，整理 outputs/ → 重命名为 outputs-{mode_desc}
 进入下一个模式
```

**输出目录结构：**

```
outputs-T0.0_FixedSeed/
├── Create a note_timestamp_1/      ← 每个实例一个目录
│   ├── trace.json                  ← 完整轨迹
│   ├── images/
│   │   ├── 0_before.jpg
│   │   ├── 0_after.jpg
│   │   └── ...
│   └── xmls/
│       └── 0_before.xml
├── Create a note_timestamp_2/
│   └── ...
├── Create a note_timestamp_3/
│   └── ...
└── logs/
    ├── Create a note_w0_timestamp.log  ← 执行日志
    ├── Create a note_w1_timestamp.log
    └── task_summary.log               ← 任务状态汇总
```

---

## 二、分析管道

### run_analysis.py — 一键分析入口

依次调用 `analyze_consistency.py` 和 `visualize_trace.py`，生成全部报告。

```bash
python run_analysis.py                           # 分析当前目录下所有 outputs-* 目录
python run_analysis.py --dir /path/to/data       # 分析指定目录
python run_analysis.py --dirs dir1 dir2          # 分析多个目录
python run_analysis.py --skip-html               # 跳过 HTML 生成
python run_analysis.py --skip-chart              # 跳过一致性分析
```

### 1. 轨迹一致性分析（analyze_consistency.py）

将同一任务在同一模式下多次运行的轨迹进行**动作序列折叠对比**，判断其行为一致性。

#### 折叠动作序列

将原始 `trace.json` 中的每一步操作转换为一个**可比较的字符串**。原始操作格式为：

```
JSONAction(action_type='click', index=3)
JSONAction(action_type='input_text', index=2, text='Ruggell, Liechtenstein')
JSONAction(action_type='scroll', index=8, direction='up')
JSONAction(action_type='open_app', app_name='Clock')
JSONAction(action_type='navigate_back')
JSONAction(action_type='status', goal_status='complete')
```

每一步都包含 `action_type`（操作类型），以及类型相关的参数：`index`（UI 控件索引）、`text`（输入文本）、`app_name`（应用名）、`direction`（滚动方向）。

新签名格式为 `action_type[_direction]@target_id`，保留 `index`（UI 控件身份标识）和 `app_name`，但不保留 `text` 内容：

| 原始操作 | L1 签名 | 保留信息 |
|:---|:---|:---|
| `click(index=3)` | `click@3` | 点击了索引为 3 的控件 |
| `long_press(index=0)` | `long_press@0` | 长按了索引为 0 的控件 |
| `input_text(index=2, text='...')` | `input_text@2` | 在索引 2 的输入框中输入（丢弃文本内容） |
| `scroll(index=8, direction='up')` | `scroll_up@8` | 在索引 8 的列表中向上滚动 |
| `swipe(direction='left')` | `swipe_left` | 向左滑动（可能无 index） |
| `open_app(app_name='Clock')` | `open_app@Clock` | 打开了 Clock 应用 |
| `navigate_back` | `navigate_back` | 返回操作 |
| `keyboard_enter` | `keyboard_enter` | 键盘确认 |
| `wait` | `wait` | 等待 |
| `status(goal_status='complete')` | `status@complete` | 任务完成状态 |


#### 目录发现规则

自动扫描以 `outputs-` 开头的目录。目录名中的温度值通过正则 `[Tt](\d+(?:\.\d+)?)` 提取，
种子模式根据目录名中的关键字判断。

#### 任务匹配

每个 `trace.json` 中的 `goal` 字段通过 `task_matcher_utils.get_task_info_by_goal()` 与
预定义的任务模板进行模糊匹配，得到 `task_name`。同一 `task_name` + 时间戳聚类（180 秒阈值）
内的多条轨迹被视为同一实验组。

#### 一致性分类规则

对每个实验组（时间戳聚类后 >1 条轨迹的组），计算两两归一化 Levenshtein 距离，按最大值分入四个等级：

| 类型 | type | 条件 | 含义 | 学术对应概念 |
|:---|:---:|:---|:---|:---|
| **完全一致** (Strict) | 0 | 所有 pair 距离 = 0 | 所有轨迹的 L1 签名完全相同 | 完全复现性 |
| **高度相似** (Highly Similar) | 1 | 最大距离 ≤ 0.15 相同轨迹＞=2/3时| 微小波动，约 8 步中至多 1 步差异或者当轨迹大于等于三条时，相同的轨迹为三分之二时也判定为高度相似| 近似复现性 |
| **中等相似** (Moderately Similar) | 2 | 0.15 < 最大距离 ≤ 0.35 | 存在可观察但非根本性的路径差异 | 部分收敛性 |
| **严重分歧** (Divergent) | 3 | 最大距离 > 0.35 | 超过三分之一路径不同，行为根本偏离 | 非复现性 |

### 阈值说明

**0.15 阈值**：在 8 步任务中对应约 1 步差异（1/8=0.125），在实际数据中大约区分「偶然的额外操作（如点击了一个无关空白区域）」。

**0.35 阈值**：模型走了明显不同的分支流程（如 A 在一次点击后完成任务，B 需要进行额外的导航操作）。


### 量化指标

除离散分类外，每个实验组还输出连续指标供统计检验：

| 指标 | 含义 |
|:---|:---|
| `n` | 组内轨迹条数 |
| `mean_distance` | 组内所有 pair 归一化编辑距离的均值 |
| `max_distance` | 最大 pair 距离（决定分类等级） |
| `std_distance` | 距离标准差（反映组内离散度） |
| `majority_ratio` | 最多数签名的占比（值域 [0,1]，=1 表示全一致） |
| `completion_match` | 组内是否所有轨迹完成状态一致 |

---

#### 输出文件

| 文件 | 说明 |
|------|------|
| `trajectory_consistency_chart.png` | 每组实验的严格/相似/分歧柱状图 |
| `trajectory_analysis_report.txt` | 详细文本报告，列出每个实验组的分类结果 |
| `trajectory_consistency.json` | 按轨迹 ID 索引的一致性 JSON，供 HTML 可视化使用 |
| `unknown_actions_report.txt` | 无法解析的动作统计 |

### 2. 分组轨迹可视化（visualize_trace.py）

生成交互式 HTML 报告，可逐组浏览所有轨迹的详细步骤。

**特性：**
- 侧边栏列出所有实验组，显示任务名、轨迹数、**一致性标签**
- 搜索过滤（按任务名、goal 等关键词）
- 主内容区以列为单位展示每条轨迹的完整执行步骤
- 每步操作自动标注动作类型（点击、输入、滚动等）
- 操作区域（click/input_text 等）在截图上用红色边框高亮
- scroll/swipe 方向用箭头标注
- input_text 内容在截图旁显示文字标注
- 操作完成/失败状态标记

**一致性标签在 HTML 中的显示位置：**
- **侧边栏**：每个 group-item 的 task_name 下方，彩色标签直接可见
- **主内容**：group-header 中 group-stats 行下方

### 3. 总索引页（index.html）
汇总所有分析结果，顶部列图像缩略图，可点击直达完整报告。
---

## 三、分析结果

```bash
python run_analysis.py
```




## 四、依赖文件

| 文件 | 职责 |
|------|------|
| `run.py` | 单次任务评测入口 |
| `parallel_runner_vllm.py` | 多实例并行调度器 |
| `run_analysis.py` | 一键分析入口 |
| `analyze_consistency.py` | 轨迹一致性分析核心 |
| `visualize_trace.py` | 交互式 HTML 报告生成 |
| `task_matcher_utils.py` | 任务匹配、时间戳聚类、标签生成工具 |
| `analysis_lib.py` | 轨迹解析、动作签名提取等通用库 |
| `trajectory_analysis_report.txt` | 分析文本报告 |
| `trajectory_consistency.json` | 一致性标记数据（供 HTML 使用） |
| `index.html` | 总索引页 |
